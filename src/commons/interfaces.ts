export interface Product {
    id: number
    name: string
    price: number
    image: string
    sizes: string[]
    colors: string[]
    rating: number
    reviews: number
  }
export interface FormData {
    name: string;
    email: string;
    phone: string;
    password: string;
  }